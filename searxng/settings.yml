# Read the documentation before extending the defaults:
# https://docs.searxng.org/admin/settings/

use_default_settings: true

server:
  # secret_key deliberately absent: it is supplied by the SEARXNG_SECRET environment
  # variable, set in the gitignored .env at the repository root. Never inline it here,
  # this file is committed.
  image_proxy: true
